{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug JourneyLogger",
            "type": "python",
            "request": "launch",
            "module": "journeylogger",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/src"
            }
        }
    ]
}